import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { PencilIcon, DuplicateIcon } from '@heroicons/react/solid';
import { DefaultLayout } from '~/mdx-layout';
export default DefaultLayout;

import { Menu } from '~/menu';

# Menu

<Description of={Menu} />

## Usage

```tsx
import { Menu } from '@divriots/starter-tailwindcss/menu';
import { PencilIcon, DuplicateIcon } from '@heroicons/react/solid';

<Menu
  items={[
    {
      Icon: PencilIcon,
      label: 'Edit',
      onClick: () => window.alert('Edit was pressed!'),
    },
    { Icon: DuplicateIcon, label: 'Duplicate' },
    { label: 'Exit' },
  ]}
/>;
```

## Example

<Playground
  className="not-prose bg-demo-menu headlessui"
  scope={{ Menu, PencilIcon, DuplicateIcon }}
  code={`
<div className="py-16 pb-40 w-max mx-auto">
  <Menu
    label='Options'
    items={[
      [
        {
          Icon: PencilIcon,
          label: 'Edit',
          onClick: () => window.alert('Edit was pressed!'),
        },
        { Icon: DuplicateIcon, label: 'Duplicate' }
      ], [
        { label: 'Exit' }
      ]
    ]}
  />
</div>
  `}
/>

## Props

<Props of={Menu} />
