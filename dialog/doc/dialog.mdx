import { mdx } from '@mdx-js/react';
import { Props, Description } from '@divriots/dockit-react/props';
import { Playground } from '@divriots/dockit-react/playground';
import { DefaultLayout } from '~/mdx-layout';
export default DefaultLayout;
import { Dialog } from '~/dialog';

# Dialog

<Description of={Dialog} />

## Usage

```tsx
import { Dialog } from '@divriots/starter-tailwindcss/dialog';

<Dialog
  openLabel="Open dialog"
  closeLabel="Got it, thanks!"
  title="Payment successful"
  details="Your payment has been successfully submitted. We’ve sent you an email with all of the details of your order."
  onOpen={() => console.log("You've opened the dialog")}
  onClose={() => console.log("You've closed the dialog")}
/>;
```

## Example

<Playground
  className="not-prose bg-demo-dialog headlessui"
  scope={{ Dialog }}
  code={`
<div className="py-16 w-max mx-auto">
  <Dialog
    openLabel="Open dialog"
    closeLabel="Got it, thanks!"
    title="Payment successful"
    details="Your payment has been successfully submitted. We’ve sent you an email with all of the details of your order."
  />
</div>
  `}
/>

## Props

<Props of={Dialog} />
